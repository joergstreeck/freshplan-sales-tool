#!/usr/bin/env sh

# ============================================================================
# Husky Pre-Commit Hook: Multi-Stage Quality Gate
# ============================================================================
#
# PRÜFUNG 1: Design System Compliance (BLOCKIEREND)
# PRÜFUNG 2: Server-Driven Architecture Parity (BLOCKIEREND)
# PRÜFUNG 2.3: Enum Seed Data Case Validation (BLOCKIEREND) - NEW Sprint 2.1.7.7
# PRÜFUNG 2.5: Enum-Rendering-Parity (BLOCKIEREND) - Sprint 2.1.7.7
# PRÜFUNG 3: Server-Driven Sections Architecture (BLOCKIEREND)
# PRÜFUNG 4: Backend Code Formatting (Spotless Auto-Format)
# PRÜFUNG 5: Backend Compilation Check (BLOCKIEREND) - Sprint 2.1.7.7
# PRÜFUNG 6: Frontend Code Formatting (Lint-Staged Auto-Format)
# PRÜFUNG 7: Dependency Cycles Check (BLOCKIEREND) - Sprint 2.1.7.7
# PRÜFUNG 8: Test Cleanup - @AfterEach Auto-Add (Auto-Apply) - Sprint 2.1.7.7
#
# ============================================================================

set -e

# Colors for output
RED='\033[0;31m'
YELLOW='\033[1;33m'
GREEN='\033[0;32m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# ============================================================================
# PRÜFUNG 1: Design System Compliance Check
# ============================================================================

echo ""
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BLUE}🎨 Design System Compliance Check${NC}"
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

# Run Design System Check (Python script)
if ! python3 ./scripts/check-design-system.py; then
  echo ""
  echo -e "${RED}🚫 PRE-COMMIT HOOK: Design System Violations detected!${NC}"
  echo ""
  echo -e "${YELLOW}   Commit BLOCKIERT. Bitte behebe die CRITICAL violations.${NC}"
  echo ""
  echo -e "${YELLOW}   Zum Überspringen (NOT RECOMMENDED): git commit --no-verify${NC}"
  echo ""
  exit 1
fi

echo ""
echo -e "${GREEN}✅ Design System Compliance: PASSED${NC}"
echo ""

# ============================================================================
# PRÜFUNG 2: Server-Driven Architecture Parity
# ============================================================================

echo ""
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BLUE}🔒 Server-Driven Architecture Parity Check${NC}"
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

# Run Server-Driven Parity Check (Python script)
if ! python3 ./scripts/check-server-driven-parity.py; then
  echo ""
  echo -e "${RED}🚫 PRE-COMMIT HOOK: Server-Driven Parity Violations detected!${NC}"
  echo ""
  echo -e "${YELLOW}   Commit BLOCKIERT. Bitte behebe die Violations:${NC}"
  echo ""
  echo -e "   ${YELLOW}• STUFE 1: Alle Schema-Felder MÜSSEN in Entity existieren${NC}"
  echo -e "   ${YELLOW}• STUFE 2: ENUM-Felder MÜSSEN enumSource haben (kein hardcoded options)${NC}"
  echo ""
  echo -e "${YELLOW}   Zum Überspringen (NOT RECOMMENDED): git commit --no-verify${NC}"
  echo ""
  exit 1
fi

echo ""
echo -e "${GREEN}✅ Server-Driven Parity: PASSED${NC}"
echo ""

# ============================================================================
# PRÜFUNG 2.3: Enum Value Case Validation in Seed Data
# ============================================================================

echo ""
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BLUE}🔢 Enum Seed Data Case Validation${NC}"
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

# Run Enum Seed Data Case Check (Python script)
if ! python3 ./scripts/check-enum-seed-data-case.py; then
  echo ""
  echo -e "${RED}🚫 PRE-COMMIT HOOK: Enum Value Case Violations detected!${NC}"
  echo ""
  echo -e "${YELLOW}   Commit BLOCKIERT. Bitte behebe die Violations:${NC}"
  echo ""
  echo -e "   ${YELLOW}• ALLE Enum-Werte in V900*.sql MÜSSEN UPPERCASE sein${NC}"
  echo -e "   ${YELLOW}• Beispiel: 'GmbH' → 'GMBH', 'Frau' → 'FRAU'${NC}"
  echo ""
  echo -e "${YELLOW}   Zum Überspringen (NOT RECOMMENDED): git commit --no-verify${NC}"
  echo ""
  exit 1
fi

echo ""
echo -e "${GREEN}✅ Enum Seed Data Case: PASSED${NC}"
echo ""

# ============================================================================
# PRÜFUNG 2.5: Enum-Rendering-Parity Check (Context-Aware)
# ============================================================================

echo ""
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BLUE}🔍 Enum-Rendering-Parity Check (Context-Aware)${NC}"
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

# Run Enum-Rendering-Parity Check (Python script)
if ! python3 ./scripts/check-enum-rendering-parity.py; then
  echo ""
  echo -e "${RED}🚫 PRE-COMMIT HOOK: Enum-Rendering-Parity Violations detected!${NC}"
  echo ""
  echo -e "${YELLOW}   Commit BLOCKIERT. Bitte behebe die RAW Enum-Rendering Violations:${NC}"
  echo ""
  echo -e "   ${YELLOW}• ZERO TOLERANCE: Backend = Single Source of Truth für ALLE Enum-Werte${NC}"
  echo -e "   ${YELLOW}• Lösung: Nutze useEnumOptions() + Label-Lookup${NC}"
  echo -e "   ${YELLOW}• Beispiel: decisionLevelLabels[contact.decisionLevel]${NC}"
  echo ""
  echo -e "${YELLOW}   Zum Überspringen (NOT RECOMMENDED): git commit --no-verify${NC}"
  echo ""
  exit 1
fi

echo ""
echo -e "${GREEN}✅ Enum-Rendering-Parity: PASSED${NC}"
echo ""

# ============================================================================
# PRÜFUNG 3: Server-Driven Sections Architecture
# ============================================================================

echo ""
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BLUE}🏗️  Server-Driven Sections Architecture Check${NC}"
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

# Run Server-Driven Sections Check (Bash script in backend/)
if [ -f "backend/scripts/check-server-driven-sections.sh" ]; then
  if ! (cd backend && ./scripts/check-server-driven-sections.sh); then
    echo ""
    echo -e "${RED}🚫 PRE-COMMIT HOOK: Server-Driven Sections Violations detected!${NC}"
    echo ""
    echo -e "${YELLOW}   Commit BLOCKIERT. Bitte behebe die Violations:${NC}"
    echo ""
    echo -e "   ${YELLOW}• STUFE 1: Wizard-Fields MÜSSEN wizardSectionId + wizardSectionTitle haben${NC}"
    echo -e "   ${YELLOW}• STUFE 2: Frontend darf KEINE hardcodierten Section-Strukturen haben${NC}"
    echo ""
    echo -e "${YELLOW}   Zum Überspringen (NOT RECOMMENDED): git commit --no-verify${NC}"
    echo ""
    exit 1
  fi

  echo ""
  echo -e "${GREEN}✅ Server-Driven Sections: PASSED${NC}"
  echo ""
else
  echo ""
  echo -e "${YELLOW}⚠️  Server-Driven Sections Check: Script not found (skipped)${NC}"
  echo ""
fi

# ============================================================================
# PRÜFUNG 4: Backend Code Formatting (Spotless Auto-Format)
# ============================================================================

echo ""
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BLUE}🧹 Backend Code Formatting (Spotless)${NC}"
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

# Check if any backend files were changed
BACKEND_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep "^backend/" || true)

if [ -n "$BACKEND_FILES" ]; then
  echo "📝 Backend files changed, applying Spotless formatting..."
  echo ""

  # Apply Spotless formatting
  if ! (cd backend && ./mvnw spotless:apply -q); then
    echo ""
    echo -e "${RED}🚫 PRE-COMMIT HOOK: Spotless formatting failed!${NC}"
    echo ""
    echo -e "${YELLOW}   Commit BLOCKIERT. Bitte prüfe die Spotless-Konfiguration.${NC}"
    echo ""
    exit 1
  fi

  # Add formatted files back to staging
  echo "✨ Re-staging formatted backend files..."
  git add backend/

  echo ""
  echo -e "${GREEN}✅ Backend Code Formatting: APPLIED${NC}"
  echo ""
else
  echo "⏭️  No backend files changed, skipping Spotless"
  echo ""
fi

# ============================================================================
# PRÜFUNG 5: Backend Compilation Check
# ============================================================================

echo ""
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BLUE}🔨 Backend Compilation Check${NC}"
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

# Check if any backend files were changed (reuse variable from PRÜFUNG 4)
BACKEND_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep "^backend/" || true)

if [ -n "$BACKEND_FILES" ]; then
  echo "🔨 Backend files changed, checking compilation..."
  echo ""

  # Run Maven compile (quiet mode)
  if ! (cd backend && ./mvnw -B compile -q -DskipTests); then
    echo ""
    echo -e "${RED}🚫 PRE-COMMIT HOOK: Backend Compilation FAILED!${NC}"
    echo ""
    echo -e "${YELLOW}   Commit BLOCKIERT. Bitte behebe die Compilation-Fehler:${NC}"
    echo ""
    echo -e "   ${YELLOW}• Prüfe Java-Syntax-Fehler${NC}"
    echo -e "   ${YELLOW}• Prüfe fehlende Imports${NC}"
    echo -e "   ${YELLOW}• Prüfe Type-Errors${NC}"
    echo ""
    echo -e "${YELLOW}   Zum Überspringen (NOT RECOMMENDED): git commit --no-verify${NC}"
    echo ""
    exit 1
  fi

  echo ""
  echo -e "${GREEN}✅ Backend Compilation: PASSED${NC}"
  echo ""
else
  echo "⏭️  No backend files changed, skipping compilation check"
  echo ""
fi

# ============================================================================
# PRÜFUNG 6: Frontend Code Formatting (Lint-Staged)
# ============================================================================

echo ""
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BLUE}💅 Frontend Code Formatting (lint-staged)${NC}"
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

# Frontend lint-staged
if [ -d "frontend" ]; then
  if ! (cd frontend && npm run lint-staged); then
    echo ""
    echo -e "${RED}🚫 PRE-COMMIT HOOK: Lint-Staged failed!${NC}"
    echo ""
    echo -e "${YELLOW}   Commit BLOCKIERT. Bitte behebe die Linting-Fehler.${NC}"
    echo ""
    exit 1
  fi

  echo ""
  echo -e "${GREEN}✅ Frontend Lint-Staged: PASSED${NC}"
  echo ""
fi

# ============================================================================
# PRÜFUNG 7: Dependency Cycles Check
# ============================================================================

echo ""
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BLUE}🔄 Dependency Cycles Check${NC}"
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

# Check if any backend files were changed
BACKEND_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep "^backend/.*\.java$" || true)

if [ -n "$BACKEND_FILES" ]; then
  echo "🔍 Backend Java files changed, checking for circular dependencies..."
  echo ""

  # Run Dependency Cycles Check (Python script)
  if ! python3 ./scripts/check-dependency-cycles.py; then
    echo ""
    echo -e "${RED}🚫 PRE-COMMIT HOOK: Dependency Cycles detected!${NC}"
    echo ""
    echo -e "${YELLOW}   Commit BLOCKIERT. Bitte behebe die zirkulären Abhängigkeiten:${NC}"
    echo ""
    echo -e "   ${YELLOW}• Lösung 1: Interface Abstraction (Dependency Inversion Principle)${NC}"
    echo -e "   ${YELLOW}• Lösung 2: Shared Package für gemeinsame Klassen${NC}"
    echo -e "   ${YELLOW}• Lösung 3: Dependency Richtung umkehren${NC}"
    echo ""
    echo -e "${YELLOW}   Details siehe oben im Check-Output.${NC}"
    echo ""
    echo -e "${YELLOW}   Zum Überspringen (NOT RECOMMENDED): git commit --no-verify${NC}"
    echo ""
    exit 1
  fi

  echo ""
  echo -e "${GREEN}✅ Dependency Cycles Check: PASSED (No cycles detected)${NC}"
  echo ""
else
  echo "⏭️  No backend Java files changed, skipping dependency cycles check"
  echo ""
fi

# ============================================================================
# PRÜFUNG 8: Test Cleanup - @AfterEach Auto-Add
# ============================================================================

echo ""
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BLUE}🧪 Test Cleanup - @AfterEach Auto-Add${NC}"
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

# Check if any backend test files were changed
BACKEND_TEST_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep "^backend/src/test/.*\.java$" || true)

if [ -n "$BACKEND_TEST_FILES" ]; then
  echo "🧪 Backend test files changed, checking for @AfterEach cleanup..."
  echo ""

  # Run Test Cleanup script (Auto-Add like Spotless)
  if ! python3 ./backend/scripts/add-test-cleanup.py; then
    echo ""
    echo -e "${RED}🚫 PRE-COMMIT HOOK: Test Cleanup failed!${NC}"
    echo ""
    echo -e "${YELLOW}   Commit BLOCKIERT. Bitte prüfe die Test-Cleanup-Konfiguration.${NC}"
    echo ""
    exit 1
  fi

  # Add modified test files back to staging
  echo "✨ Re-staging modified test files..."
  git add backend/src/test/

  echo ""
  echo -e "${GREEN}✅ Test Cleanup: @AfterEach methods ensured${NC}"
  echo ""
else
  echo "⏭️  No backend test files changed, skipping test cleanup check"
  echo ""
fi

# ============================================================================
# SUCCESS
# ============================================================================

echo ""
echo -e "${GREEN}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${GREEN}✅ All Pre-Commit Checks PASSED${NC}"
echo -e "${GREEN}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

exit 0

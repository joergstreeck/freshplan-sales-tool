# ===================================
# FreshPlan Sales Tool - Environment Variables
# ===================================
# Copy this file to .env and update with your values
# NEVER commit .env to version control!

# ----- Keycloak Configuration -----
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=changeme_in_production
POSTGRES_PASSWORD=changeme_in_production
KEYCLOAK_DB_PASSWORD=changeme_in_production

# ----- Backend API -----
VITE_API_URL=http://localhost:8080
QUARKUS_HTTP_PORT=8080

# ----- Frontend Configuration -----
VITE_USE_MSW=false
VITE_PORT=5173

# ----- Database -----
DB_HOST=localhost
DB_PORT=5432
DB_NAME=freshplan
DB_USER=freshplan
DB_PASSWORD=changeme_in_production

# ----- Security -----
JWT_SECRET=changeme_use_strong_secret_in_production
SESSION_SECRET=changeme_use_strong_secret_in_production

# ----- Development Only -----
# Set to true ONLY for local development without backend
# VITE_USE_MSW=true
# Test Environment Configuration Example
# Copy this file to .env.test and update with your test values

# E2E Test User Credentials
# These should be test-only accounts, never real user credentials!
VITE_TEST_USER_EMAIL=e2e@test.example.com
VITE_TEST_USER_PASSWORD=your-test-password-here

# Test API Configuration (if different from development)
# VITE_API_URL=http://localhost:8080/api

# Test Keycloak Configuration (if using test realm)
# VITE_KEYCLOAK_URL=http://localhost:8180
# VITE_KEYCLOAK_REALM=test-realm
# VITE_KEYCLOAK_CLIENT=test-client
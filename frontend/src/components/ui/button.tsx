/**
 * ⚠️ LEGACY REDIRECT: Diese Datei leitet auf die neue Transition-Komponente um
 *
 * Ursprünglich war das die alte shadcn Komponente.
 * Jetzt wird auf button-transition.tsx umgeleitet.
 *
 * Diese Datei bleibt nur für Kompatibilität bestehen.
 */

// Re-export everything from button-transition
export { Button, buttonVariants } from './button-transition';
export type { ButtonProps } from './button-transition';
export type { VariantProps } from 'class-variance-authority';

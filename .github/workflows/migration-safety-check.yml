name: Migration Safety Check

on:
  push:
    branches: ['**']
    paths:
      - 'backend/src/main/resources/db/migration/V*.sql'
      - 'backend/src/main/resources/db/dev-migration/V*.sql'
  pull_request:
    branches: ['**']
    paths:
      - 'backend/src/main/resources/db/migration/V*.sql'
      - 'backend/src/main/resources/db/dev-migration/V*.sql'

jobs:
  check-migrations:
    name: Check Migration Files
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Volle History für Vergleiche

      - name: Check Migration Folders
        run: |
          echo "🔍 Prüfe Migration-Ordner..."

          # Sammle alle Migrations-Dateien
          MIGRATION_FILES=$(find backend/src/main/resources/db/migration -name "V*.sql" 2>/dev/null || echo "")
          DEV_FILES=$(find backend/src/main/resources/db/dev-migration -name "V*.sql" 2>/dev/null || echo "")

          ERROR=0

          # Prüfe migration/ (Production)
          if [ ! -z "$MIGRATION_FILES" ]; then
            echo "📁 Prüfe migration/ (Production)..."
            for FILE in $MIGRATION_FILES; do
              FILENAME=$(basename "$FILE")

              # Exception: V19 ist bereits deployed (seit 2025-08-08)
              if [ "$FILENAME" = "V19__add_test_data_flag.sql" ]; then
                echo "   ⚠️  $FILENAME (Exception: bereits deployed)"
                continue
              fi

              # Prüfe auf Test-Keywords
              if echo "$FILENAME" | grep -qiE "(test|demo|seed|sample|debug)"; then
                echo "❌ FEHLER: $FILENAME enthält Test-Keywords!"
                echo "   Test-Migrationen gehören in dev-migration/!"
                ERROR=1
              fi
            done
          fi

          # Prüfe dev-migration/ (Test/Dev)
          if [ ! -z "$DEV_FILES" ]; then
            echo "📁 Prüfe dev-migration/ (Test/Dev)..."
            for FILE in $DEV_FILES; do
              FILENAME=$(basename "$FILE")
              echo "   ✓ $FILENAME (Test-Migration)"
            done
          fi

          if [ $ERROR -eq 1 ]; then
            echo ""
            echo "❌ Migration Safety Check FEHLGESCHLAGEN!"
            exit 1
          fi

          echo "✅ Alle Ordner-Prüfungen bestanden!"

      - name: Check Migration Numbers
        run: |
          echo "🔢 Prüfe Migration-Nummern..."

          # Sammle alle Nummern (aus BEIDEN Ordnern)
          ALL_VERSIONS=$(find backend/src/main/resources/db/migration backend/src/main/resources/db/dev-migration \
            -name "V*.sql" 2>/dev/null | \
            sed 's/.*V\([0-9]*\)__.*/\1/' | sort -n | uniq)

          if [ -z "$ALL_VERSIONS" ]; then
            echo "ℹ️  Keine Migrationen gefunden"
            exit 0
          fi

          # Prüfe aufsteigende Reihenfolge
          PREV=0
          ERROR=0
          for VERSION in $ALL_VERSIONS; do
            if [ "$VERSION" -le "$PREV" ]; then
              echo "❌ FEHLER: V$VERSION ist nicht aufsteigend!"
              echo "   Vorherige: V$PREV"
              ERROR=1
            fi
            PREV=$VERSION
          done

          if [ $ERROR -eq 1 ]; then
            echo ""
            echo "❌ Nummern-Prüfung FEHLGESCHLAGEN!"
            exit 1
          fi

          echo "✅ Alle Nummern aufsteigend! (V1 bis V$PREV)"

      - name: Summary
        if: success()
        run: |
          echo "🎉 Migration Safety Check BESTANDEN!"
          echo ""
          echo "Alle Prüfungen erfolgreich:"
          echo "  ✅ Ordner-Struktur korrekt"
          echo "  ✅ Keine Test-Keywords in migration/"
          echo "  ✅ Migration-Nummern aufsteigend"
